#include <assert.h>
#include <stdlib.h>

#include "arithmetic.h"

VariableType *arithmetic_addition(VariableType *val1, VariableType *val2)
{
    assert(val1 != NULL);
    assert(val2 != NULL);

    return NULL;
}
